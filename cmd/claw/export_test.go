package main

// Colorize exposes the colorize function for external testing.
var Colorize = colorize

// StatusColors exposes the statusColors map for external testing.
var StatusColors = statusColors

// ColorReset exposes the colorReset constant for external testing.
const ColorReset = colorReset
